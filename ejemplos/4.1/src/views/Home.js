/**
 * Pagina principal
 */
const Home = () => (
  <section aria-describedby="desc">
    <p id="desc">
      React-router es una librería para la gestión de rutas en tus aplicaciones
      de React. La gran ventaja de React-router es su simpleza.
    </p>
  </section>
);

export default Home;